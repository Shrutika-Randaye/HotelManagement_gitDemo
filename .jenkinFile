pipeline {
    agent any

    tools{
maven "MAVEN_HOME"
}
stage('Build') {
 steps {
                // Check out the Git repository
                git 
            }
            steps {
                // Build the Maven project
                sh 'mvn clean install'
            }
        }
